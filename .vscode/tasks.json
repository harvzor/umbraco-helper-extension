{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            // Used for quickly debugging extension in Firefox.
            // You will need to run `npm install web-ext -g` for this.
            "taskName": "Web Extension Run",
            "type": "shell",
            "command": "web-ext run",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}